package com.wonder.provider.model

/** A generated explore pick opened on the map with directions from the traveller. */
data class ExplorePlacePick(
    val title: String,
    val query: String,
    val context: String
)
